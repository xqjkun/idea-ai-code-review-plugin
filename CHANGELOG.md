# 更新记录

## 0.5.2（2026-08-25）

### 发布

- 新增 GitHub Actions 自动发布流程：推送与项目版本一致的 `v*` Tag 后，自动执行测试、构建和 IDEA 兼容性验证。
- 验证通过后自动创建 GitHub Release，并上传可直接安装的插件 ZIP。
- 发布流程覆盖 IntelliJ IDEA 2024.1 至 2026.2 共 8 个目标版本。

## 0.5.1（2026-08-24）

### 修复

- 报告查看后的放行条件改为比较完整 Commit Diff 指纹，不再仅比较文件集合。
- 只查看代码且 Diff 完全未变化时，再次 Commit 不重复调用 DeepSeek，写入 `AI-Review: reviewed-no-change` 后继续其他检查。
- 修改任意代码、新增文件或删除文件时，上次报告立即失效，清除查看标记并使用最新 Diff 重新调用 DeepSeek。

## 0.5.0（2026-08-24）

### 新增

- AI 阻断报告增加“去修改并保留报告”，报告窗口改为非模态，可一边查看问题、一边修改代码。
- 修改完成后再次点击 Commit，同一批文件在 30 分钟内跳过一次 AI 复审，直接继续 IDEA Code Analysis、Git Hook 等其他提交检查。
- 手工修复流程会在 Commit Message 中写入 `AI-Review: fixed-after-review`，便于在 GitLab 中审计。
- 提交成功后自动恢复正常 AI 审核；提交文件集合变化或豁免超时会重新调用 AI。

## 0.4.2（2026-08-21）

### 修复

- 修复点击审核问题中的“打开代码”后，代码编辑器被模态审核窗口遮挡、无法获得焦点的问题。
- 操作名称调整为“打开并定位代码”。点击后先关闭当前审核窗口，再打开对应文件。
- 定位成功后自动把光标移动到报告行，将目标位置滚动到编辑器中央，并获取编辑器焦点。
- 如果 AI 返回的文件路径不准确或文件已被删除，继续显示明确的文件查找失败提示。

### 行为说明

- “仍然提交”只豁免本次 AI 审核结果，并继续 IDEA 的其他提交检查。
- 插件不会使用 `git commit --no-verify`，因此不会主动跳过 Git Hook。
- IDEA Code Analysis 是否允许继续提交仍由 IDEA 自身设置和用户选择决定。需要不可绕过的质量门禁时，应在 GitLab CI 中执行编译、静态检查和测试。

## 0.4.1（2026-08-21）

### 新增

- 识别 DeepSeek 官方 HTTP 402 余额不足响应。
- 兼容部分 AI 网关返回的 `insufficient_balance`、`insufficient_quota`、中文“余额不足/额度不足”等响应。
- 余额不足时显示“本次代码未经过 AI 审核”的警告，但允许本次提交继续执行。
- 普通 HTTP 429 限流不会被误判为余额不足。

### 保持不变

- API Key 错误、网络失败、响应格式错误等普通异常仍会中止提交，并允许开发者人工复核后仅豁免 AI 审核。

## 0.4.0（2026-08-21）

### 新增

- AI 审核摘要、问题说明、修复建议和测试建议改为 Markdown 富文本显示。
- 支持标题、无序/有序列表、引用、粗体、斜体、删除线、行内代码和代码块。
- 富文本显示适配 IDEA 深色和浅色主题，并自动换行。
- AI 返回的原始 HTML 会被转义，避免模型输出影响插件界面。
- Markdown 渲染不依赖 IDEA Markdown 插件，保持跨版本兼容。

## 0.3.9（2026-08-21）

### 新增

- 审核发现改为结构化问题卡片，按严重级别显示不同颜色。
- 每个问题增加文件路径、行号和“打开代码”入口。
- 强制提交必须填写原因并确认已经人工复核。
- 强制提交原因自动写入 Git Commit Message：

  ```text
  AI-Review: overridden
  AI-Review-Reason: 人工复核原因
  ```

- 自动兼容已有 Git Trailer，例如 `Signed-off-by`，并避免重复追加 AI Trailer。

### 审核能力

- 审核范围限定为本次实际选中的 Commit changes，不继承上一次提交内容。
- 同时采集修改前、修改后和变更点附近逻辑，用于判断状态、金额、字段及调用关系。
- 根据 `import`、`require` 等引用补充有限的直接依赖和调用方，支持跨文件勾稽审核。
- 大上下文请求增加空响应诊断、输出截断识别、JSON 协议重试和 Token 信息提示。
- API Key 保存到 IntelliJ Password Safe，并支持旧版本凭据自动迁移，升级插件不需要重新填写 Key。

### 兼容性

- 同一个安装包支持 IntelliJ IDEA 2024.1 至 2026.2，平台构建号范围为 `241`–`262.*`。
- 插件使用 Java 17 字节码构建。

## 早期版本

- 完成 DeepSeek 提交前代码审核的基础流程。
- 支持按本次 Commit 的实际选中改动生成 unified patch。
- 支持 `CRITICAL`、`HIGH` 阻断以及可选的 `MEDIUM` 阻断。
- 支持每位开发者单独配置 DeepSeek API 地址、模型和 API Key。
- 增加 Diff 大小、文件数量、上下文大小及关联文件数量限制，防止超大提交造成模型输出截断。
